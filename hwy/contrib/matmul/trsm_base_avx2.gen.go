// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build amd64 && goexperiment.simd

package matmul

import (
	"simd/archsimd"
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseTrsmLN_avx2_Float16(l []hwy.Float16, b []hwy.Float16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 8
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := asm.BroadcastFloat16x8AVX2(uint16(-lij))
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := asm.LoadFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k]))
				vx := asm.LoadFloat16x8AVX2Ptr(unsafe.Pointer(&b[j*nrhs+k]))
				negLij.MulAdd(vx, vb).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() - lij.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := asm.BroadcastFloat16x8AVX2(uint16(hwy.Float16(1) / l[i*n+i]))
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			asm.LoadFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k])).Mul(invLii).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
		}
		s := hwy.Float32ToFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLN_avx2_BFloat16(l []hwy.BFloat16, b []hwy.BFloat16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 8
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := asm.BroadcastBFloat16x8AVX2(uint16(-lij))
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := asm.LoadBFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k]))
				vx := asm.LoadBFloat16x8AVX2Ptr(unsafe.Pointer(&b[j*nrhs+k]))
				negLij.MulAdd(vx, vb).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() - lij.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := asm.BroadcastBFloat16x8AVX2(uint16(hwy.BFloat16(1) / l[i*n+i]))
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			asm.LoadBFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k])).Mul(invLii).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
		}
		s := hwy.Float32ToBFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLN_avx2(l []float32, b []float32, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 8
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := archsimd.BroadcastFloat32x8(-lij)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k])))
				vx := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&b[j*nrhs+k])))
				negLij.MulAdd(vx, vb).Store((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k])))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lij * b[j*nrhs+k]
			}
		}
		invLii := archsimd.BroadcastFloat32x8(float32(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k]))).Mul(invLii).Store((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k])))
		}
		s := float32(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}

func BaseTrsmLN_avx2_Float64(l []float64, b []float64, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 4
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := archsimd.BroadcastFloat64x4(-lij)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k])))
				vx := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&b[j*nrhs+k])))
				negLij.MulAdd(vx, vb).Store((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k])))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lij * b[j*nrhs+k]
			}
		}
		invLii := archsimd.BroadcastFloat64x4(float64(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k]))).Mul(invLii).Store((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k])))
		}
		s := float64(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}

func BaseTrsmLT_avx2_Float16(l []hwy.Float16, b []hwy.Float16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 8
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := asm.BroadcastFloat16x8AVX2(uint16(-lji))
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := asm.LoadFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k]))
				vx := asm.LoadFloat16x8AVX2Ptr(unsafe.Pointer(&b[j*nrhs+k]))
				negLji.MulAdd(vx, vb).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() - lji.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := asm.BroadcastFloat16x8AVX2(uint16(hwy.Float16(1) / l[i*n+i]))
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			asm.LoadFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k])).Mul(invLii).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
		}
		s := hwy.Float32ToFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLT_avx2_BFloat16(l []hwy.BFloat16, b []hwy.BFloat16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 8
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := asm.BroadcastBFloat16x8AVX2(uint16(-lji))
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := asm.LoadBFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k]))
				vx := asm.LoadBFloat16x8AVX2Ptr(unsafe.Pointer(&b[j*nrhs+k]))
				negLji.MulAdd(vx, vb).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() - lji.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := asm.BroadcastBFloat16x8AVX2(uint16(hwy.BFloat16(1) / l[i*n+i]))
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			asm.LoadBFloat16x8AVX2Ptr(unsafe.Pointer(&b[i*nrhs+k])).Mul(invLii).StorePtr(unsafe.Pointer(&b[i*nrhs+k]))
		}
		s := hwy.Float32ToBFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLT_avx2(l []float32, b []float32, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 8
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := archsimd.BroadcastFloat32x8(-lji)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k])))
				vx := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&b[j*nrhs+k])))
				negLji.MulAdd(vx, vb).Store((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k])))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lji * b[j*nrhs+k]
			}
		}
		invLii := archsimd.BroadcastFloat32x8(float32(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k]))).Mul(invLii).Store((*[8]float32)(unsafe.Pointer(&b[i*nrhs+k])))
		}
		s := float32(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}

func BaseTrsmLT_avx2_Float64(l []float64, b []float64, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := 4
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := archsimd.BroadcastFloat64x4(-lji)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k])))
				vx := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&b[j*nrhs+k])))
				negLji.MulAdd(vx, vb).Store((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k])))
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lji * b[j*nrhs+k]
			}
		}
		invLii := archsimd.BroadcastFloat64x4(float64(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k]))).Mul(invLii).Store((*[4]float64)(unsafe.Pointer(&b[i*nrhs+k])))
		}
		s := float64(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}
