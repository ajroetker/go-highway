// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package matmul

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseTrsmLN_fallback_Float16(l []hwy.Float16, b []hwy.Float16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := hwy.Zero[hwy.Float16]().NumLanes()
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := hwy.Set(-lij)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := hwy.Load(b[i*nrhs+k:])
				vx := hwy.Load(b[j*nrhs+k:])
				hwy.Store(hwy.MulAdd(negLij, vx, vb), b[i*nrhs+k:])
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() - lij.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := hwy.Set(hwy.Float16(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			hwy.Store(hwy.Mul(hwy.Load(b[i*nrhs+k:]), invLii), b[i*nrhs+k:])
		}
		s := hwy.Float32ToFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLN_fallback_BFloat16(l []hwy.BFloat16, b []hwy.BFloat16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := hwy.Zero[hwy.BFloat16]().NumLanes()
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := hwy.Set(-lij)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := hwy.Load(b[i*nrhs+k:])
				vx := hwy.Load(b[j*nrhs+k:])
				hwy.Store(hwy.MulAdd(negLij, vx, vb), b[i*nrhs+k:])
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() - lij.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := hwy.Set(hwy.BFloat16(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			hwy.Store(hwy.Mul(hwy.Load(b[i*nrhs+k:]), invLii), b[i*nrhs+k:])
		}
		s := hwy.Float32ToBFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLN_fallback(l []float32, b []float32, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := float32(-lij)
			var k int
			for k = 0; k < nrhs; k++ {
				vb := b[i*nrhs+k]
				vx := b[j*nrhs+k]
				b[i*nrhs+k] = negLij*vx + vb
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lij * b[j*nrhs+k]
			}
		}
		invLii := float32(float32(1) / l[i*n+i])
		var k int
		for k = 0; k < nrhs; k++ {
			b[i*nrhs+k] = b[i*nrhs+k] * invLii
		}
		s := float32(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}

func BaseTrsmLN_fallback_Float64(l []float64, b []float64, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	for i := range n {
		for j := range i {
			lij := l[i*n+j]
			negLij := float64(-lij)
			var k int
			for k = 0; k < nrhs; k++ {
				vb := b[i*nrhs+k]
				vx := b[j*nrhs+k]
				b[i*nrhs+k] = negLij*vx + vb
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lij * b[j*nrhs+k]
			}
		}
		invLii := float64(float64(1) / l[i*n+i])
		var k int
		for k = 0; k < nrhs; k++ {
			b[i*nrhs+k] = b[i*nrhs+k] * invLii
		}
		s := float64(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}

func BaseTrsmLT_fallback_Float16(l []hwy.Float16, b []hwy.Float16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := hwy.Zero[hwy.Float16]().NumLanes()
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := hwy.Set(-lji)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := hwy.Load(b[i*nrhs+k:])
				vx := hwy.Load(b[j*nrhs+k:])
				hwy.Store(hwy.MulAdd(negLji, vx, vb), b[i*nrhs+k:])
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() - lji.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := hwy.Set(hwy.Float16(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			hwy.Store(hwy.Mul(hwy.Load(b[i*nrhs+k:]), invLii), b[i*nrhs+k:])
		}
		s := hwy.Float32ToFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLT_fallback_BFloat16(l []hwy.BFloat16, b []hwy.BFloat16, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	lanes := hwy.Zero[hwy.BFloat16]().NumLanes()
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := hwy.Set(-lji)
			var k int
			for k = 0; k+lanes <= nrhs; k += lanes {
				vb := hwy.Load(b[i*nrhs+k:])
				vx := hwy.Load(b[j*nrhs+k:])
				hwy.Store(hwy.MulAdd(negLji, vx, vb), b[i*nrhs+k:])
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() - lji.Float32()*b[j*nrhs+k].Float32())
			}
		}
		invLii := hwy.Set(hwy.BFloat16(1) / l[i*n+i])
		var k int
		for k = 0; k+lanes <= nrhs; k += lanes {
			hwy.Store(hwy.Mul(hwy.Load(b[i*nrhs+k:]), invLii), b[i*nrhs+k:])
		}
		s := hwy.Float32ToBFloat16(float32(1) / l[i*n+i].Float32())
		for ; k < nrhs; k++ {
			b[i*nrhs+k] = hwy.Float32ToBFloat16(b[i*nrhs+k].Float32() * s.Float32())
		}
	}
}

func BaseTrsmLT_fallback(l []float32, b []float32, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := float32(-lji)
			var k int
			for k = 0; k < nrhs; k++ {
				vb := b[i*nrhs+k]
				vx := b[j*nrhs+k]
				b[i*nrhs+k] = negLji*vx + vb
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lji * b[j*nrhs+k]
			}
		}
		invLii := float32(float32(1) / l[i*n+i])
		var k int
		for k = 0; k < nrhs; k++ {
			b[i*nrhs+k] = b[i*nrhs+k] * invLii
		}
		s := float32(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}

func BaseTrsmLT_fallback_Float64(l []float64, b []float64, n int, nrhs int) {
	if n == 0 || nrhs == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsm: L slice too short")
	}
	if len(b) < n*nrhs {
		panic("trsm: B slice too short")
	}
	for i := n - 1; i >= 0; i-- {
		for j := i + 1; j < n; j++ {
			lji := l[j*n+i]
			negLji := float64(-lji)
			var k int
			for k = 0; k < nrhs; k++ {
				vb := b[i*nrhs+k]
				vx := b[j*nrhs+k]
				b[i*nrhs+k] = negLji*vx + vb
			}
			for ; k < nrhs; k++ {
				b[i*nrhs+k] -= lji * b[j*nrhs+k]
			}
		}
		invLii := float64(float64(1) / l[i*n+i])
		var k int
		for k = 0; k < nrhs; k++ {
			b[i*nrhs+k] = b[i*nrhs+k] * invLii
		}
		s := float64(1) / l[i*n+i]
		for ; k < nrhs; k++ {
			b[i*nrhs+k] *= s
		}
	}
}
