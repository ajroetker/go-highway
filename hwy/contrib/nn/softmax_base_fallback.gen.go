// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package nn

import (
	stdmath "math"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/contrib/math"
)

func BaseSoftmax_fallback_Float16(input []hwy.Float16, output []hwy.Float16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	lanes := hwy.MaxLanes[hwy.Float16]()
	vMax := hwy.Set(maxVal)
	sumAcc := hwy.Zero[hwy.Float16]()
	i := 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Sub(x, vMax)
		expVal := math.BaseExpVec_fallback_Float16(shifted)
		hwy.StoreSlice(expVal, output[i:])
		sumAcc = hwy.Add(sumAcc, expVal)
	}
	expSum := hwy.ReduceSum(sumAcc).Float32()
	for ; i < size; i++ {
		val := hwy.Float32ToFloat16(float32(stdmath.Exp(float64(input[i].Float32() - maxVal.Float32()))))
		output[i] = hwy.Float32ToFloat16(val.Float32())
		expSum += val.Float32()
	}
	invSum := hwy.Float32ToFloat16(float32(1.0) / expSum)
	vInvSum := hwy.Set(invSum)
	i = 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(output[i:])
		result := hwy.Mul(x, vInvSum)
		hwy.StoreSlice(result, output[i:])
	}
	for ; i < size; i++ {
		output[i] = hwy.Float32ToFloat16(output[i].Float32() * invSum.Float32())
	}
}

func BaseSoftmax_fallback_BFloat16(input []hwy.BFloat16, output []hwy.BFloat16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	lanes := hwy.MaxLanes[hwy.BFloat16]()
	vMax := hwy.Set(maxVal)
	sumAcc := hwy.Zero[hwy.BFloat16]()
	i := 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Sub(x, vMax)
		expVal := math.BaseExpVec_fallback_BFloat16(shifted)
		hwy.StoreSlice(expVal, output[i:])
		sumAcc = hwy.Add(sumAcc, expVal)
	}
	expSum := hwy.ReduceSum(sumAcc).Float32()
	for ; i < size; i++ {
		val := hwy.Float32ToBFloat16(float32(stdmath.Exp(float64(input[i].Float32() - maxVal.Float32()))))
		output[i] = hwy.Float32ToBFloat16(val.Float32())
		expSum += val.Float32()
	}
	invSum := hwy.Float32ToBFloat16(float32(1.0) / expSum)
	vInvSum := hwy.Set(invSum)
	i = 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(output[i:])
		result := hwy.Mul(x, vInvSum)
		hwy.StoreSlice(result, output[i:])
	}
	for ; i < size; i++ {
		output[i] = hwy.Float32ToBFloat16(output[i].Float32() * invSum.Float32())
	}
}

func BaseSoftmax_fallback(input []float32, output []float32) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	vMax := float32(maxVal)
	sumAcc := float32(0)
	i := 0
	for ; i < size; i++ {
		x := input[i]
		shifted := x - vMax
		expVal := float32(stdmath.Exp(float64(shifted)))
		output[i] = expVal
		sumAcc = sumAcc + expVal
	}
	expSum := sumAcc
	for ; i < size; i++ {
		val := float32(stdmath.Exp(float64(input[i] - maxVal)))
		output[i] = val
		expSum += val
	}
	invSum := float32(1.0) / expSum
	vInvSum := float32(invSum)
	i = 0
	for ; i < size; i++ {
		x := output[i]
		result := x * vInvSum
		output[i] = result
	}
	for ; i < size; i++ {
		output[i] = output[i] * invSum
	}
}

func BaseSoftmax_fallback_Float64(input []float64, output []float64) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	vMax := float64(maxVal)
	sumAcc := float64(0)
	i := 0
	for ; i < size; i++ {
		x := input[i]
		shifted := x - vMax
		expVal := float64(stdmath.Exp(float64(shifted)))
		output[i] = expVal
		sumAcc = sumAcc + expVal
	}
	expSum := sumAcc
	for ; i < size; i++ {
		val := float64(stdmath.Exp(float64(input[i] - maxVal)))
		output[i] = val
		expSum += val
	}
	invSum := float64(1.0) / expSum
	vInvSum := float64(invSum)
	i = 0
	for ; i < size; i++ {
		x := output[i]
		result := x * vInvSum
		output[i] = result
	}
	for ; i < size; i++ {
		output[i] = output[i] * invSum
	}
}

func BaseSoftmaxInPlace_fallback_Float16(x []hwy.Float16) {
	BaseSoftmax_fallback_Float16(x, x)
}

func BaseSoftmaxInPlace_fallback_BFloat16(x []hwy.BFloat16) {
	BaseSoftmax_fallback_BFloat16(x, x)
}

func BaseSoftmaxInPlace_fallback(x []float32) {
	BaseSoftmax_fallback(x, x)
}

func BaseSoftmaxInPlace_fallback_Float64(x []float64) {
	BaseSoftmax_fallback_Float64(x, x)
}

func BaseLogSoftmax_fallback_Float16(input []hwy.Float16, output []hwy.Float16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	lanes := hwy.MaxLanes[hwy.Float16]()
	vMax := hwy.Set(maxVal)
	sumAcc := hwy.Zero[hwy.Float16]()
	i := 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Sub(x, vMax)
		sumAcc = hwy.Add(sumAcc, math.BaseExpVec_fallback_Float16(shifted))
	}
	expSum := hwy.ReduceSum(sumAcc).Float32()
	for ; i < size; i++ {
		expSum += float32(stdmath.Exp(float64(input[i].Float32() - maxVal.Float32())))
	}
	logSumExp := hwy.Float32ToFloat16(float32(stdmath.Log(float64(expSum))))
	vLogSumExp := hwy.Set(logSumExp)
	i = 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Sub(x, vMax)
		result := hwy.Sub(shifted, vLogSumExp)
		hwy.StoreSlice(result, output[i:])
	}
	for ; i < size; i++ {
		output[i] = hwy.Float32ToFloat16((input[i].Float32() - maxVal.Float32()) - logSumExp.Float32())
	}
}

func BaseLogSoftmax_fallback_BFloat16(input []hwy.BFloat16, output []hwy.BFloat16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	lanes := hwy.MaxLanes[hwy.BFloat16]()
	vMax := hwy.Set(maxVal)
	sumAcc := hwy.Zero[hwy.BFloat16]()
	i := 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Sub(x, vMax)
		sumAcc = hwy.Add(sumAcc, math.BaseExpVec_fallback_BFloat16(shifted))
	}
	expSum := hwy.ReduceSum(sumAcc).Float32()
	for ; i < size; i++ {
		expSum += float32(stdmath.Exp(float64(input[i].Float32() - maxVal.Float32())))
	}
	logSumExp := hwy.Float32ToBFloat16(float32(stdmath.Log(float64(expSum))))
	vLogSumExp := hwy.Set(logSumExp)
	i = 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Sub(x, vMax)
		result := hwy.Sub(shifted, vLogSumExp)
		hwy.StoreSlice(result, output[i:])
	}
	for ; i < size; i++ {
		output[i] = hwy.Float32ToBFloat16((input[i].Float32() - maxVal.Float32()) - logSumExp.Float32())
	}
}

func BaseLogSoftmax_fallback(input []float32, output []float32) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	vMax := float32(maxVal)
	sumAcc := float32(0)
	i := 0
	for ; i < size; i++ {
		x := input[i]
		shifted := x - vMax
		sumAcc = sumAcc + float32(stdmath.Exp(float64(shifted)))
	}
	expSum := sumAcc
	for ; i < size; i++ {
		expSum += float32(stdmath.Exp(float64(input[i] - maxVal)))
	}
	logSumExp := float32(stdmath.Log(float64(expSum)))
	vLogSumExp := float32(logSumExp)
	i = 0
	for ; i < size; i++ {
		x := input[i]
		shifted := x - vMax
		result := shifted - vLogSumExp
		output[i] = result
	}
	for ; i < size; i++ {
		output[i] = (input[i] - maxVal) - logSumExp
	}
}

func BaseLogSoftmax_fallback_Float64(input []float64, output []float64) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	vMax := float64(maxVal)
	sumAcc := float64(0)
	i := 0
	for ; i < size; i++ {
		x := input[i]
		shifted := x - vMax
		sumAcc = sumAcc + float64(stdmath.Exp(float64(shifted)))
	}
	expSum := sumAcc
	for ; i < size; i++ {
		expSum += float64(stdmath.Exp(float64(input[i] - maxVal)))
	}
	logSumExp := float64(stdmath.Log(float64(expSum)))
	vLogSumExp := float64(logSumExp)
	i = 0
	for ; i < size; i++ {
		x := input[i]
		shifted := x - vMax
		result := shifted - vLogSumExp
		output[i] = result
	}
	for ; i < size; i++ {
		output[i] = (input[i] - maxVal) - logSumExp
	}
}

func BaseLogSoftmaxInPlace_fallback_Float16(x []hwy.Float16) {
	BaseLogSoftmax_fallback_Float16(x, x)
}

func BaseLogSoftmaxInPlace_fallback_BFloat16(x []hwy.BFloat16) {
	BaseLogSoftmax_fallback_BFloat16(x, x)
}

func BaseLogSoftmaxInPlace_fallback(x []float32) {
	BaseLogSoftmax_fallback(x, x)
}

func BaseLogSoftmaxInPlace_fallback_Float64(x []float64) {
	BaseLogSoftmax_fallback_Float64(x, x)
}

func BaseSoftmaxScalar_fallback_Float16(input []hwy.Float16, output []hwy.Float16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	var expSum float32
	for i := range size {
		output[i] = hwy.Float32ToFloat16(float32(stdmath.Exp(float64(input[i].Float32() - maxVal.Float32()))))
		expSum += output[i].Float32()
	}
	invSum := hwy.Float32ToFloat16(float32(1.0) / expSum)
	for i := range size {
		output[i] = hwy.Float32ToFloat16(output[i].Float32() * invSum.Float32())
	}
}

func BaseSoftmaxScalar_fallback_BFloat16(input []hwy.BFloat16, output []hwy.BFloat16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	var expSum float32
	for i := range size {
		output[i] = hwy.Float32ToBFloat16(float32(stdmath.Exp(float64(input[i].Float32() - maxVal.Float32()))))
		expSum += output[i].Float32()
	}
	invSum := hwy.Float32ToBFloat16(float32(1.0) / expSum)
	for i := range size {
		output[i] = hwy.Float32ToBFloat16(output[i].Float32() * invSum.Float32())
	}
}

func BaseSoftmaxScalar_fallback(input []float32, output []float32) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	var expSum float32
	for i := range size {
		output[i] = float32(stdmath.Exp(float64(input[i] - maxVal)))
		expSum += output[i]
	}
	invSum := float32(1.0) / expSum
	for i := range size {
		output[i] = output[i] * invSum
	}
}

func BaseSoftmaxScalar_fallback_Float64(input []float64, output []float64) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	var expSum float64
	for i := range size {
		output[i] = float64(stdmath.Exp(float64(input[i] - maxVal)))
		expSum += output[i]
	}
	invSum := float64(1.0) / expSum
	for i := range size {
		output[i] = output[i] * invSum
	}
}

func BaseSoftmaxWithTemperature_fallback_Float16(input []hwy.Float16, output []hwy.Float16, temperature hwy.Float16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	lanes := hwy.MaxLanes[hwy.Float16]()
	invTemp := hwy.Float32ToFloat16(float32(1.0) / temperature.Float32())
	vMax := hwy.Set(maxVal)
	vInvTemp := hwy.Set(invTemp)
	sumAcc := hwy.Zero[hwy.Float16]()
	i := 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Mul(hwy.Sub(x, vMax), vInvTemp)
		expVal := math.BaseExpVec_fallback_Float16(shifted)
		hwy.StoreSlice(expVal, output[i:])
		sumAcc = hwy.Add(sumAcc, expVal)
	}
	expSum := hwy.ReduceSum(sumAcc).Float32()
	for ; i < size; i++ {
		val := hwy.Float32ToFloat16(float32(stdmath.Exp(float64((input[i].Float32() - maxVal.Float32()) * invTemp.Float32()))))
		output[i] = hwy.Float32ToFloat16(val.Float32())
		expSum += val.Float32()
	}
	invSum := hwy.Float32ToFloat16(float32(1.0) / expSum)
	vInvSum := hwy.Set(invSum)
	i = 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(output[i:])
		result := hwy.Mul(x, vInvSum)
		hwy.StoreSlice(result, output[i:])
	}
	for ; i < size; i++ {
		output[i] = hwy.Float32ToFloat16(output[i].Float32() * invSum.Float32())
	}
}

func BaseSoftmaxWithTemperature_fallback_BFloat16(input []hwy.BFloat16, output []hwy.BFloat16, temperature hwy.BFloat16) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j].Float32() > maxVal.Float32() {
			maxVal = input[j]
		}
	}
	lanes := hwy.MaxLanes[hwy.BFloat16]()
	invTemp := hwy.Float32ToBFloat16(float32(1.0) / temperature.Float32())
	vMax := hwy.Set(maxVal)
	vInvTemp := hwy.Set(invTemp)
	sumAcc := hwy.Zero[hwy.BFloat16]()
	i := 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(input[i:])
		shifted := hwy.Mul(hwy.Sub(x, vMax), vInvTemp)
		expVal := math.BaseExpVec_fallback_BFloat16(shifted)
		hwy.StoreSlice(expVal, output[i:])
		sumAcc = hwy.Add(sumAcc, expVal)
	}
	expSum := hwy.ReduceSum(sumAcc).Float32()
	for ; i < size; i++ {
		val := hwy.Float32ToBFloat16(float32(stdmath.Exp(float64((input[i].Float32() - maxVal.Float32()) * invTemp.Float32()))))
		output[i] = hwy.Float32ToBFloat16(val.Float32())
		expSum += val.Float32()
	}
	invSum := hwy.Float32ToBFloat16(float32(1.0) / expSum)
	vInvSum := hwy.Set(invSum)
	i = 0
	for ; i+lanes <= size; i += lanes {
		x := hwy.LoadSlice(output[i:])
		result := hwy.Mul(x, vInvSum)
		hwy.StoreSlice(result, output[i:])
	}
	for ; i < size; i++ {
		output[i] = hwy.Float32ToBFloat16(output[i].Float32() * invSum.Float32())
	}
}

func BaseSoftmaxWithTemperature_fallback(input []float32, output []float32, temperature float32) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	invTemp := float32(1.0) / temperature
	vMax := float32(maxVal)
	vInvTemp := float32(invTemp)
	sumAcc := float32(0)
	i := 0
	for ; i < size; i++ {
		x := input[i]
		shifted := (x - vMax) * vInvTemp
		expVal := float32(stdmath.Exp(float64(shifted)))
		output[i] = expVal
		sumAcc = sumAcc + expVal
	}
	expSum := sumAcc
	for ; i < size; i++ {
		val := float32(stdmath.Exp(float64((input[i] - maxVal) * invTemp)))
		output[i] = val
		expSum += val
	}
	invSum := float32(1.0) / expSum
	vInvSum := float32(invSum)
	i = 0
	for ; i < size; i++ {
		x := output[i]
		result := x * vInvSum
		output[i] = result
	}
	for ; i < size; i++ {
		output[i] = output[i] * invSum
	}
}

func BaseSoftmaxWithTemperature_fallback_Float64(input []float64, output []float64, temperature float64) {
	size := min(len(input), len(output))
	if size == 0 {
		return
	}
	maxVal := input[0]
	for j := 1; j < size; j++ {
		if input[j] > maxVal {
			maxVal = input[j]
		}
	}
	invTemp := float64(1.0) / temperature
	vMax := float64(maxVal)
	vInvTemp := float64(invTemp)
	sumAcc := float64(0)
	i := 0
	for ; i < size; i++ {
		x := input[i]
		shifted := (x - vMax) * vInvTemp
		expVal := float64(stdmath.Exp(float64(shifted)))
		output[i] = expVal
		sumAcc = sumAcc + expVal
	}
	expSum := sumAcc
	for ; i < size; i++ {
		val := float64(stdmath.Exp(float64((input[i] - maxVal) * invTemp)))
		output[i] = val
		expSum += val
	}
	invSum := float64(1.0) / expSum
	vInvSum := float64(invSum)
	i = 0
	for ; i < size; i++ {
		x := output[i]
		result := x * vInvSum
		output[i] = result
	}
	for ; i < size; i++ {
		output[i] = output[i] * invSum
	}
}
