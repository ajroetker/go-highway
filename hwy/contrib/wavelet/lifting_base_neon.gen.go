// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build arm64

package wavelet

import (
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

// Hoisted constants - pre-broadcasted at package init time
var (
	BaseLiftUpdate53_NEON_twoVec_f32     = asm.BroadcastInt64x2(int64(2))
	BaseLiftUpdate53_NEON_twoVec_i32_f32 = asm.BroadcastInt32x4(int32(2))
)

func BaseLiftUpdate53_neon_Int32(target []int32, tLen int, neighbor []int32, nLen int, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	twoVec := BaseLiftUpdate53_NEON_twoVec_i32_f32
	lanes := 4
	off1, off2 := -1, 0
	if phase == 1 {
		off1, off2 = 0, 1
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] -= (neighbor[n1Idx] + neighbor[n2Idx] + 2) >> 2
	}
	_ = twoVec
	_ = lanes
}

func BaseLiftUpdate53_neon_Int64(target []int64, tLen int, neighbor []int64, nLen int, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	twoVec := BaseLiftUpdate53_NEON_twoVec_f32
	lanes := 2
	off1, off2 := -1, 0
	if phase == 1 {
		off1, off2 = 0, 1
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] -= (neighbor[n1Idx] + neighbor[n2Idx] + 2) >> 2
	}
	_ = twoVec
	_ = lanes
}

func BaseLiftPredict53_neon_Int32(target []int32, tLen int, neighbor []int32, nLen int, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	lanes := 4
	off1, off2 := 0, 1
	if phase == 1 {
		off1, off2 = -1, 0
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] += (neighbor[n1Idx] + neighbor[n2Idx]) >> 1
	}
	_ = lanes
}

func BaseLiftPredict53_neon_Int64(target []int64, tLen int, neighbor []int64, nLen int, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	lanes := 2
	off1, off2 := 0, 1
	if phase == 1 {
		off1, off2 = -1, 0
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] += (neighbor[n1Idx] + neighbor[n2Idx]) >> 1
	}
	_ = lanes
}

func BaseLiftStep97_neon_Float16(target []hwy.Float16, tLen int, neighbor []hwy.Float16, nLen int, coeff hwy.Float16, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	coeffVec := asm.BroadcastFloat16x8(uint16(coeff))
	lanes := 8
	off1, off2 := 0, 1
	if phase == 1 {
		off1, off2 = -1, 0
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] = hwy.Float32ToFloat16(target[i].Float32() - coeff.Float32()*(neighbor[n1Idx].Float32()+neighbor[n2Idx].Float32()))
	}
	_ = coeffVec
	_ = lanes
}

func BaseLiftStep97_neon_BFloat16(target []hwy.BFloat16, tLen int, neighbor []hwy.BFloat16, nLen int, coeff hwy.BFloat16, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	coeffVec := asm.BroadcastBFloat16x8(uint16(coeff))
	lanes := 8
	off1, off2 := 0, 1
	if phase == 1 {
		off1, off2 = -1, 0
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] = hwy.Float32ToBFloat16(target[i].Float32() - coeff.Float32()*(neighbor[n1Idx].Float32()+neighbor[n2Idx].Float32()))
	}
	_ = coeffVec
	_ = lanes
}

func BaseLiftStep97_neon(target []float32, tLen int, neighbor []float32, nLen int, coeff float32, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	coeffVec := asm.BroadcastFloat32x4(coeff)
	lanes := 4
	off1, off2 := 0, 1
	if phase == 1 {
		off1, off2 = -1, 0
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] -= coeff * (neighbor[n1Idx] + neighbor[n2Idx])
	}
	_ = coeffVec
	_ = lanes
}

func BaseLiftStep97_neon_Float64(target []float64, tLen int, neighbor []float64, nLen int, coeff float64, phase int) {
	if tLen == 0 || nLen == 0 {
		return
	}
	coeffVec := asm.BroadcastFloat64x2(coeff)
	lanes := 2
	off1, off2 := 0, 1
	if phase == 1 {
		off1, off2 = -1, 0
	}
	for i := 0; i < tLen; i++ {
		n1Idx := i + off1
		n2Idx := i + off2
		if n1Idx < 0 {
			n1Idx = 0
		}
		if n1Idx >= nLen {
			n1Idx = nLen - 1
		}
		if n2Idx < 0 {
			n2Idx = 0
		}
		if n2Idx >= nLen {
			n2Idx = nLen - 1
		}
		target[i] -= coeff * (neighbor[n1Idx] + neighbor[n2Idx])
	}
	_ = coeffVec
	_ = lanes
}

func BaseScaleSlice_neon_Float16(data []hwy.Float16, n int, scale hwy.Float16) {
	if n == 0 || data == nil {
		return
	}
	scaleVec := asm.BroadcastFloat16x8(uint16(scale))
	lanes := 8
	i := 0
	for ; i+lanes*4 <= n; i += lanes * 4 {
		v := asm.LoadFloat16x8Ptr(unsafe.Pointer(&data[i:][0]))
		result := v.Mul(scaleVec)
		result.StorePtr(unsafe.Pointer(&data[i:][0]))
		v1 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&data[i+8:][0]))
		result1 := v1.Mul(scaleVec)
		result1.StorePtr(unsafe.Pointer(&data[i+8:][0]))
		v2 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&data[i+16:][0]))
		result2 := v2.Mul(scaleVec)
		result2.StorePtr(unsafe.Pointer(&data[i+16:][0]))
		v3 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&data[i+24:][0]))
		result3 := v3.Mul(scaleVec)
		result3.StorePtr(unsafe.Pointer(&data[i+24:][0]))
	}
	for ; i+lanes <= n; i += lanes {
		v := asm.LoadFloat16x8Ptr(unsafe.Pointer(&data[i:][0]))
		result := v.Mul(scaleVec)
		result.StorePtr(unsafe.Pointer(&data[i:][0]))
	}
	if remaining := n - i; remaining > 0 {
		buf := [8]hwy.Float16{}
		copy(buf[:], data[i:i+remaining])
		v := asm.LoadFloat16x8Ptr(unsafe.Pointer(&buf[0]))
		result := v.Mul(scaleVec)
		result.StorePtr(unsafe.Pointer(&buf[0]))
		copy(data[i:i+remaining], buf[:remaining])
	}
}

func BaseScaleSlice_neon_BFloat16(data []hwy.BFloat16, n int, scale hwy.BFloat16) {
	if n == 0 || data == nil {
		return
	}
	scaleVec := asm.BroadcastBFloat16x8(uint16(scale))
	lanes := 8
	i := 0
	for ; i+lanes*4 <= n; i += lanes * 4 {
		v := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&data[i:][0]))
		result := v.Mul(scaleVec)
		result.StorePtr(unsafe.Pointer(&data[i:][0]))
		v1 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&data[i+8:][0]))
		result1 := v1.Mul(scaleVec)
		result1.StorePtr(unsafe.Pointer(&data[i+8:][0]))
		v2 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&data[i+16:][0]))
		result2 := v2.Mul(scaleVec)
		result2.StorePtr(unsafe.Pointer(&data[i+16:][0]))
		v3 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&data[i+24:][0]))
		result3 := v3.Mul(scaleVec)
		result3.StorePtr(unsafe.Pointer(&data[i+24:][0]))
	}
	for ; i+lanes <= n; i += lanes {
		v := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&data[i:][0]))
		result := v.Mul(scaleVec)
		result.StorePtr(unsafe.Pointer(&data[i:][0]))
	}
	if remaining := n - i; remaining > 0 {
		buf := [8]hwy.BFloat16{}
		copy(buf[:], data[i:i+remaining])
		v := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&buf[0]))
		result := v.Mul(scaleVec)
		result.StorePtr(unsafe.Pointer(&buf[0]))
		copy(data[i:i+remaining], buf[:remaining])
	}
}

func BaseScaleSlice_neon(data []float32, n int, scale float32) {
	if n == 0 || data == nil {
		return
	}
	scaleVec := asm.BroadcastFloat32x4(scale)
	lanes := 4
	i := 0
	for ; i+lanes*4 <= n; i += lanes * 4 {
		v := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&data[i])))
		result := v.Mul(scaleVec)
		result.Store((*[4]float32)(unsafe.Pointer(&data[i])))
		v1 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&data[i+4])))
		result1 := v1.Mul(scaleVec)
		result1.Store((*[4]float32)(unsafe.Pointer(&data[i+4])))
		v2 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&data[i+8])))
		result2 := v2.Mul(scaleVec)
		result2.Store((*[4]float32)(unsafe.Pointer(&data[i+8])))
		v3 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&data[i+12])))
		result3 := v3.Mul(scaleVec)
		result3.Store((*[4]float32)(unsafe.Pointer(&data[i+12])))
	}
	for ; i+lanes <= n; i += lanes {
		v := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&data[i])))
		result := v.Mul(scaleVec)
		result.Store((*[4]float32)(unsafe.Pointer(&data[i])))
	}
	if remaining := n - i; remaining > 0 {
		buf := [4]float32{}
		copy(buf[:], data[i:i+remaining])
		v := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&buf[0])))
		result := v.Mul(scaleVec)
		result.Store((*[4]float32)(unsafe.Pointer(&buf[0])))
		copy(data[i:i+remaining], buf[:remaining])
	}
}

func BaseScaleSlice_neon_Float64(data []float64, n int, scale float64) {
	if n == 0 || data == nil {
		return
	}
	scaleVec := asm.BroadcastFloat64x2(scale)
	lanes := 2
	i := 0
	for ; i+lanes*4 <= n; i += lanes * 4 {
		v := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&data[i])))
		result := v.Mul(scaleVec)
		result.Store((*[2]float64)(unsafe.Pointer(&data[i])))
		v1 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&data[i+2])))
		result1 := v1.Mul(scaleVec)
		result1.Store((*[2]float64)(unsafe.Pointer(&data[i+2])))
		v2 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&data[i+4])))
		result2 := v2.Mul(scaleVec)
		result2.Store((*[2]float64)(unsafe.Pointer(&data[i+4])))
		v3 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&data[i+6])))
		result3 := v3.Mul(scaleVec)
		result3.Store((*[2]float64)(unsafe.Pointer(&data[i+6])))
	}
	for ; i+lanes <= n; i += lanes {
		v := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&data[i])))
		result := v.Mul(scaleVec)
		result.Store((*[2]float64)(unsafe.Pointer(&data[i])))
	}
	if remaining := n - i; remaining > 0 {
		buf := [2]float64{}
		copy(buf[:], data[i:i+remaining])
		v := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&buf[0])))
		result := v.Mul(scaleVec)
		result.Store((*[2]float64)(unsafe.Pointer(&buf[0])))
		copy(data[i:i+remaining], buf[:remaining])
	}
}

func BaseInterleave_neon(dst []float32, low []float32, sn int, high []float32, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = low[i]
			dst[2*i+1] = high[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i+1] = high[i]
		}
	} else {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = high[i]
			dst[2*i+1] = low[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i+1] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i] = high[i]
		}
	}
}

func BaseInterleave_neon_Float64(dst []float64, low []float64, sn int, high []float64, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = low[i]
			dst[2*i+1] = high[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i+1] = high[i]
		}
	} else {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = high[i]
			dst[2*i+1] = low[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i+1] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i] = high[i]
		}
	}
}

func BaseInterleave_neon_Int32(dst []int32, low []int32, sn int, high []int32, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = low[i]
			dst[2*i+1] = high[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i+1] = high[i]
		}
	} else {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = high[i]
			dst[2*i+1] = low[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i+1] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i] = high[i]
		}
	}
}

func BaseInterleave_neon_Int64(dst []int64, low []int64, sn int, high []int64, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = low[i]
			dst[2*i+1] = high[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i+1] = high[i]
		}
	} else {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = high[i]
			dst[2*i+1] = low[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i+1] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i] = high[i]
		}
	}
}

func BaseInterleave_neon_Uint32(dst []uint32, low []uint32, sn int, high []uint32, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = low[i]
			dst[2*i+1] = high[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i+1] = high[i]
		}
	} else {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = high[i]
			dst[2*i+1] = low[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i+1] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i] = high[i]
		}
	}
}

func BaseInterleave_neon_Uint64(dst []uint64, low []uint64, sn int, high []uint64, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = low[i]
			dst[2*i+1] = high[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i+1] = high[i]
		}
	} else {
		for i := 0; i < sn && i < dn; i++ {
			dst[2*i] = high[i]
			dst[2*i+1] = low[i]
		}
		for i := dn; i < sn; i++ {
			dst[2*i+1] = low[i]
		}
		for i := sn; i < dn; i++ {
			dst[2*i] = high[i]
		}
	}
}

func BaseDeinterleave_neon(src []float32, low []float32, sn int, high []float32, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn; i++ {
			low[i] = src[2*i]
		}
		for i := 0; i < dn; i++ {
			high[i] = src[2*i+1]
		}
	} else {
		for i := 0; i < dn; i++ {
			high[i] = src[2*i]
		}
		for i := 0; i < sn; i++ {
			low[i] = src[2*i+1]
		}
	}
}

func BaseDeinterleave_neon_Float64(src []float64, low []float64, sn int, high []float64, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn; i++ {
			low[i] = src[2*i]
		}
		for i := 0; i < dn; i++ {
			high[i] = src[2*i+1]
		}
	} else {
		for i := 0; i < dn; i++ {
			high[i] = src[2*i]
		}
		for i := 0; i < sn; i++ {
			low[i] = src[2*i+1]
		}
	}
}

func BaseDeinterleave_neon_Int32(src []int32, low []int32, sn int, high []int32, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn; i++ {
			low[i] = src[2*i]
		}
		for i := 0; i < dn; i++ {
			high[i] = src[2*i+1]
		}
	} else {
		for i := 0; i < dn; i++ {
			high[i] = src[2*i]
		}
		for i := 0; i < sn; i++ {
			low[i] = src[2*i+1]
		}
	}
}

func BaseDeinterleave_neon_Int64(src []int64, low []int64, sn int, high []int64, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn; i++ {
			low[i] = src[2*i]
		}
		for i := 0; i < dn; i++ {
			high[i] = src[2*i+1]
		}
	} else {
		for i := 0; i < dn; i++ {
			high[i] = src[2*i]
		}
		for i := 0; i < sn; i++ {
			low[i] = src[2*i+1]
		}
	}
}

func BaseDeinterleave_neon_Uint32(src []uint32, low []uint32, sn int, high []uint32, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn; i++ {
			low[i] = src[2*i]
		}
		for i := 0; i < dn; i++ {
			high[i] = src[2*i+1]
		}
	} else {
		for i := 0; i < dn; i++ {
			high[i] = src[2*i]
		}
		for i := 0; i < sn; i++ {
			low[i] = src[2*i+1]
		}
	}
}

func BaseDeinterleave_neon_Uint64(src []uint64, low []uint64, sn int, high []uint64, dn int, phase int) {
	if phase == 0 {
		for i := 0; i < sn; i++ {
			low[i] = src[2*i]
		}
		for i := 0; i < dn; i++ {
			high[i] = src[2*i+1]
		}
	} else {
		for i := 0; i < dn; i++ {
			high[i] = src[2*i]
		}
		for i := 0; i < sn; i++ {
			low[i] = src[2*i+1]
		}
	}
}
