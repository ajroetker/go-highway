//go:build !noasm && arm64

// Code generated by hwygen -c. DO NOT EDIT.

package matvec

import (
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/contrib/matvec/asm"
)

func symvLNAsmF16(a, x, y []hwy.Float16, n int) {
	if len(a) < n*n {
		panic("symv: A slice too short")
	}
	if len(x) < n || len(y) < n {
		panic("symv: vector slice too short")
	}
	var p_a unsafe.Pointer
	if len(a) > 0 {
		p_a = unsafe.Pointer(&a[0])
	}
	var p_x unsafe.Pointer
	if len(x) > 0 {
		p_x = unsafe.Pointer(&x[0])
	}
	var p_y unsafe.Pointer
	if len(y) > 0 {
		p_y = unsafe.Pointer(&y[0])
	}
	nVal := int64(n)
	len_aVal := int64(len(a))
	len_xVal := int64(len(x))
	len_yVal := int64(len(y))
	asm.SymvLN_F16(
		p_a,
		p_x,
		p_y,
		unsafe.Pointer(&nVal),
		unsafe.Pointer(&len_aVal),
		unsafe.Pointer(&len_xVal),
		unsafe.Pointer(&len_yVal),
	)
}

func symvLNAsmBF16(a, x, y []hwy.BFloat16, n int) {
	if len(a) < n*n {
		panic("symv: A slice too short")
	}
	if len(x) < n || len(y) < n {
		panic("symv: vector slice too short")
	}
	var p_a unsafe.Pointer
	if len(a) > 0 {
		p_a = unsafe.Pointer(&a[0])
	}
	var p_x unsafe.Pointer
	if len(x) > 0 {
		p_x = unsafe.Pointer(&x[0])
	}
	var p_y unsafe.Pointer
	if len(y) > 0 {
		p_y = unsafe.Pointer(&y[0])
	}
	nVal := int64(n)
	len_aVal := int64(len(a))
	len_xVal := int64(len(x))
	len_yVal := int64(len(y))
	asm.SymvLN_BF16(
		p_a,
		p_x,
		p_y,
		unsafe.Pointer(&nVal),
		unsafe.Pointer(&len_aVal),
		unsafe.Pointer(&len_xVal),
		unsafe.Pointer(&len_yVal),
	)
}

func symvLNAsmF32(a, x, y []float32, n int) {
	if len(a) < n*n {
		panic("symv: A slice too short")
	}
	if len(x) < n || len(y) < n {
		panic("symv: vector slice too short")
	}
	var p_a unsafe.Pointer
	if len(a) > 0 {
		p_a = unsafe.Pointer(&a[0])
	}
	var p_x unsafe.Pointer
	if len(x) > 0 {
		p_x = unsafe.Pointer(&x[0])
	}
	var p_y unsafe.Pointer
	if len(y) > 0 {
		p_y = unsafe.Pointer(&y[0])
	}
	nVal := int64(n)
	len_aVal := int64(len(a))
	len_xVal := int64(len(x))
	len_yVal := int64(len(y))
	asm.SymvLN_F32(
		p_a,
		p_x,
		p_y,
		unsafe.Pointer(&nVal),
		unsafe.Pointer(&len_aVal),
		unsafe.Pointer(&len_xVal),
		unsafe.Pointer(&len_yVal),
	)
}

func symvLNAsmF64(a, x, y []float64, n int) {
	if len(a) < n*n {
		panic("symv: A slice too short")
	}
	if len(x) < n || len(y) < n {
		panic("symv: vector slice too short")
	}
	var p_a unsafe.Pointer
	if len(a) > 0 {
		p_a = unsafe.Pointer(&a[0])
	}
	var p_x unsafe.Pointer
	if len(x) > 0 {
		p_x = unsafe.Pointer(&x[0])
	}
	var p_y unsafe.Pointer
	if len(y) > 0 {
		p_y = unsafe.Pointer(&y[0])
	}
	nVal := int64(n)
	len_aVal := int64(len(a))
	len_xVal := int64(len(x))
	len_yVal := int64(len(y))
	asm.SymvLN_F64(
		p_a,
		p_x,
		p_y,
		unsafe.Pointer(&nVal),
		unsafe.Pointer(&len_aVal),
		unsafe.Pointer(&len_xVal),
		unsafe.Pointer(&len_yVal),
	)
}
