// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package matvec

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseTrsvLN_fallback_Float16(l []hwy.Float16, b []hwy.Float16, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	lanes := hwy.Zero[hwy.Float16]().NumLanes()
	for i := range n {
		acc := hwy.Zero[hwy.Float16]()
		lRow := l[i*n:]
		var j int
		for j = 0; j+lanes <= i; j += lanes {
			acc = hwy.MulAdd(hwy.Load(lRow[j:]), hwy.Load(b[j:]), acc)
		}
		s := float64(hwy.ReduceSum(acc))
		for ; j < i; j++ {
			s += float64(lRow[j].Float32()) * float64(b[j].Float32())
		}
		b[i] = hwy.Float32ToFloat16(float32((float64(b[i].Float32()) - s) / float64(l[i*n+i].Float32())))
	}
}

func BaseTrsvLN_fallback_BFloat16(l []hwy.BFloat16, b []hwy.BFloat16, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	lanes := hwy.Zero[hwy.BFloat16]().NumLanes()
	for i := range n {
		acc := hwy.Zero[hwy.BFloat16]()
		lRow := l[i*n:]
		var j int
		for j = 0; j+lanes <= i; j += lanes {
			acc = hwy.MulAdd(hwy.Load(lRow[j:]), hwy.Load(b[j:]), acc)
		}
		s := float64(hwy.ReduceSum(acc))
		for ; j < i; j++ {
			s += float64(lRow[j].Float32()) * float64(b[j].Float32())
		}
		b[i] = hwy.Float32ToBFloat16(float32((float64(b[i].Float32()) - s) / float64(l[i*n+i].Float32())))
	}
}

func BaseTrsvLN_fallback(l []float32, b []float32, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	for i := range n {
		acc := float32(0)
		lRow := l[i*n:]
		var j int
		for j = 0; j < i; j++ {
			acc = lRow[j]*b[j] + acc
		}
		s := float64(acc)
		for ; j < i; j++ {
			s += float64(lRow[j]) * float64(b[j])
		}
		b[i] = float32((float64(b[i]) - s) / float64(l[i*n+i]))
	}
}

func BaseTrsvLN_fallback_Float64(l []float64, b []float64, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	for i := range n {
		acc := float64(0)
		lRow := l[i*n:]
		var j int
		for j = 0; j < i; j++ {
			acc = lRow[j]*b[j] + acc
		}
		s := float64(acc)
		for ; j < i; j++ {
			s += float64(lRow[j]) * float64(b[j])
		}
		b[i] = float64((float64(b[i]) - s) / float64(l[i*n+i]))
	}
}

func BaseTrsvLT_fallback_Float16(l []hwy.Float16, b []hwy.Float16, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	lanes := hwy.Zero[hwy.Float16]().NumLanes()
	for i := n - 1; i >= 0; i-- {
		b[i] = hwy.Float32ToFloat16(float32(float64(b[i].Float32()) / float64(l[i*n+i].Float32())))
		xi := b[i]
		negXi := hwy.Set(-xi)
		lRow := l[i*n:]
		var j int
		for j = 0; j+lanes <= i; j += lanes {
			vb := hwy.Load(b[j:])
			vl := hwy.Load(lRow[j:])
			hwy.Store(hwy.MulAdd(negXi, vl, vb), b[j:])
		}
		for ; j < i; j++ {
			b[j] = hwy.Float32ToFloat16(b[j].Float32() - xi.Float32()*lRow[j].Float32())
		}
	}
}

func BaseTrsvLT_fallback_BFloat16(l []hwy.BFloat16, b []hwy.BFloat16, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	lanes := hwy.Zero[hwy.BFloat16]().NumLanes()
	for i := n - 1; i >= 0; i-- {
		b[i] = hwy.Float32ToBFloat16(float32(float64(b[i].Float32()) / float64(l[i*n+i].Float32())))
		xi := b[i]
		negXi := hwy.Set(-xi)
		lRow := l[i*n:]
		var j int
		for j = 0; j+lanes <= i; j += lanes {
			vb := hwy.Load(b[j:])
			vl := hwy.Load(lRow[j:])
			hwy.Store(hwy.MulAdd(negXi, vl, vb), b[j:])
		}
		for ; j < i; j++ {
			b[j] = hwy.Float32ToBFloat16(b[j].Float32() - xi.Float32()*lRow[j].Float32())
		}
	}
}

func BaseTrsvLT_fallback(l []float32, b []float32, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	for i := n - 1; i >= 0; i-- {
		b[i] = float32(float64(b[i]) / float64(l[i*n+i]))
		xi := b[i]
		negXi := float32(-xi)
		lRow := l[i*n:]
		var j int
		for j = 0; j < i; j++ {
			vb := b[j]
			vl := lRow[j]
			b[j] = negXi*vl + vb
		}
		for ; j < i; j++ {
			b[j] -= xi * lRow[j]
		}
	}
}

func BaseTrsvLT_fallback_Float64(l []float64, b []float64, n int) {
	if n == 0 {
		return
	}
	if len(l) < n*n {
		panic("trsv: L slice too short")
	}
	if len(b) < n {
		panic("trsv: b slice too short")
	}
	for i := n - 1; i >= 0; i-- {
		b[i] = float64(float64(b[i]) / float64(l[i*n+i]))
		xi := b[i]
		negXi := float64(-xi)
		lRow := l[i*n:]
		var j int
		for j = 0; j < i; j++ {
			vb := b[j]
			vl := lRow[j]
			b[j] = negXi*vl + vb
		}
		for ; j < i; j++ {
			b[j] -= xi * lRow[j]
		}
	}
}
